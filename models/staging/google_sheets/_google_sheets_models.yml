version: 2

models:
  - name: stg_google_sheets__budget
    config:
      contract:
        enforced: false
    columns:
      - name: _row
        tests:
          - not_null
        data_type: text
        constraints:
          - type: not_null
      - name: product_id         
      - name: quantity    
      - name: month
      - name: date_load
      